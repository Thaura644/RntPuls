# Backend API base URL (no trailing slash)
# Leave unset in development — defaults to http://localhost:8080/api
# In production, set to your public API URL (e.g. https://yourdomain.com/api)
VITE_API_URL=http://localhost:8080/api
